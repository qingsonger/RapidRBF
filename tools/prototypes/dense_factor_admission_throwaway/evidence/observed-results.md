# Observed dense-factor admission disposition

This is the negative resolution evidence for
[Admit the canonical dense-factor corpus for mechanism-panel use](https://github.com/qingsonger/RapidRBF/issues/36).
It does not admit a production factor path.

## Verdict

- admission disposition: `NOT_ADMITTED`;
- mechanism input authority: `DIAGNOSTIC_ONLY`;
- downstream decision authority: `BLOCKED`;
- bounded run-scoped recompute fallback: not available;
- bounded factor-free fallback: identity/no-factor, restricted to the
  registered M1-M4 1k diagnostic ablation.

The disposition is bound to corpus
`ac282ee95062b4463d2e0a0c0ca83da454660e0e5048fa79ea3a07da280ef26e`,
negative-evidence bundle
`e526df4301141baeed695618c3c7cc9330465065bf3306231f5b6d3ad50141a4`,
the permanent
[captured dense-factor replay](https://github.com/qingsonger/RapidRBF/commit/b00160b318d2d1faf27ef6f305960ccadd3061eb),
and the permanent
[M3 assembly diagnosis](https://github.com/qingsonger/RapidRBF/commit/5be79c562b848d3f8bc020784d6ac9b2d0551fcf).

## Why promotion fails closed

The corpus identity and canonical M3 representation are usable, but the corpus
contains only a maximum-order fine block and level-0 coarse block derived from
each 10k case. It is not the complete registered M1-M4 1k/10k hierarchy and
does not carry the coordinate/model fixtures needed to certify physical
value/gradient corrections.

All five ticket gates remain unclosed:

| Gate | State | Consequence |
|---|---|---|
| hash-bound semantic-rank certificates | `EVIDENCE_MISSING` | backend factor health cannot admit `P`, `Q^T A Q`, or coarse `P_top` |
| versioned factor-health profile | `EVIDENCE_MISSING` | 30/30 observations remain `COLLECTED, UNJUDGED` |
| independent evaluator and atomic publication | `EVIDENCE_MISSING` | no factor is `ValidatedFactor` and no correction is externally `Solved` |
| resource and caller thread lease | `EVIDENCE_MISSING` | no aggregate 1/2/8-lane mechanism grant is enforceable |
| atomic factor pack/reload/reuse | `PARTIAL` | faer owns projected `B` components, but the complete `B` + `P_top` path and witnesses are absent |

[Diagnose and adjudicate the M3 frozen-literal assembly-sensitivity signal](https://github.com/qingsonger/RapidRBF/issues/35)
made `canonical-row-channel-map` the sole M3 authority. The two frozen-literal
records remain defect fixtures and are excluded from every factor input and
expected semantic result.

## Permitted downstream use

The mechanism-panel prototype may force-replay hash-verified canonical blocks
in fresh private workers to collect pivots, reconstruction, residual,
trajectory, work, and failure diagnostics. Nalgebra and exact Windows oneMKL
remain sidecar comparators. Those observations may not publish/cache a
validated factor, declare convergence, select settings, advance a survivor, or
name a production backend.

The only bounded fallback available without factor-admission implications is
the already registered identity/no-factor ablation at 1k. It is diagnostic
only and cannot resolve
[Compare restarted FGMRES and same-hierarchy RAS variants on the mechanism panel](https://github.com/qingsonger/RapidRBF/issues/32).

## Newly sharp prerequisites

Two successor questions are now precise:

1. materialize the complete canonical 1k/10k hierarchy/factor manifest,
   independent rank/nullspace certificates, physical evaluator fixtures, and
   deterministic controls;
2. then qualify a run-scoped faer factor path with a preregistered health
   profile, complete projected/coarse factor ownership, atomic witnesses, and
   caller-owned resource/thread lanes.

Persistent storage policy and the 100k rung remain outside this ticket.
