# THROWAWAY PROTOTYPE — ready-gated refinement witness replacement

## Question

Does one exact materialization and sole complete attempt of the frozen
`REPLACEMENT_CONTROLLER_EXECUTION_PLAN_FROZEN` plan validly judge the unchanged
double-double refinement witness route on all four required targets and the
frozen `1/12`, `2/12`, and `8/16` profiles?

This is Issue 53 decision evidence, not production solver code. It starts from
captured commit `7bd33ea` and binds replacement-plan SHA-256
`08036fb07eb581b5fce2664066956640be45cae136068ad69bb7b972e3f306ba`.
The candidate, dependencies, feature set, six witness sources, 18 RHS
identities, double-double boundary, thresholds, resource controls, and accepted
reference remain unchanged.

Each zero-entry lane builds the candidate only for byte identity. The
ready-gated synthetic helper cannot be sampled or released until its entry
marker proves that all requested threads reached their barrier. Every pure
trace, helper build, named helper, fast exit, injected invalidity, cleanup
result, and global check is atomically persisted before global PASS; all 277
checks and the exact helper stdout/stderr bytes must close on every lane.

Only after all four preflights pass does the same workflow attempt enter the
candidate. There is one attempt and no automatic, partial, mixed, repaired,
favorable, or replacement retry. Any evidence invalidity returns to a fresh
Wayfinder diagnosis ticket.

This prototype does not admit faer, qd, a factor route, or the factor corpus;
run the mechanism panel; choose persistent factor storage; enter the 100k rung;
or unblock downstream solver comparison.

## Run

The one command that starts the sole four-target attempt is the first push of
the exact materialization commit:

```text
git push -u origin codex/execute-ready-gated-refinement-witness
```

Do not rerun or push a repair commit under this plan identity.

Review a captured cohort in one command:

```text
python tools/prototypes/double_double_refinement_witness_throwaway/tui.py <cohort-summary.json>
```

Render the review non-interactively:

```text
python tools/prototypes/double_double_refinement_witness_throwaway/tui.py <cohort-summary.json> --snapshot
```

The TUI stores review state only in memory. The only dispositions are
`REFINEMENT_ROUTE_SUPPORTED_FOR_FULL_CORPUS_PLAN`,
`REFINEMENT_ROUTE_REJECTED_DIAGNOSTIC_ONLY`, and `INVALID_UNJUDGED`.
