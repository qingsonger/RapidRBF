[project]
name = "rapidrbf-hierarchy-admission-throwaway"
version = "0.0.0"
description = "Throwaway semantic-rank and canonical-nullspace admission prototype"
requires-python = "==3.13.*"
dependencies = [
    "numpy==2.3.2",
    "threadpoolctl==3.6.0",
]

[tool.uv]
package = false

[tool.ruff.lint.isort]
known-first-party = ["certify", "exact_rank"]
